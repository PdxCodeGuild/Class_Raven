venv_environment was made using `python -m venv env`. Project dependencies are stored in `requirements.txt` which is generated using `pip freeze > requirements.txt` and can be installed using `pipenv install`.

